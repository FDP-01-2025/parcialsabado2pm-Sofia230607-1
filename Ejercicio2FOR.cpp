#include <iostream>
using namespace std;

int main(void){
    int numero, nprimos;

    cout << "Ingrese un número: " << endl;
    cin >> numero;

    for (int numero=1; j<=nprimos, j++)
    {
        cout << "Escribe un numero: " << numero << 100 << endl;
        cin >> nprimos;
    }

    if (nrpimos)
    {
        cout << "Es un numero primo" << endl;
        cin >> nprimos;
    }

    cout << "Error, el numero ingresado no es numero primo" << endl;
    return 0;
}
